{{ with secret (printf "secret/data/platform/%s/postgres" (env "ENV")) }}
POSTGRES_PASSWORD={{ .Data.data.root_password }}
{{ end }}
{{ with secret (printf "secret/data/platform/%s/redis" (env "ENV")) }}
REDIS_PASSWORD={{ .Data.data.password }}
{{ end }}
