global:
  scrape_interval: 30s
  evaluation_interval: 30s

# Alertmanager configuration (placeholder)
alerting:
  alertmanagers:
    - static_configs:
        - targets: []

# Rule files
rule_files: []

# Scrape configs
scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:8080']
