{{ with secret (printf "secret/data/platform/%s/postgres" (env "ENV")) }}
AP_POSTGRES_PASSWORD={{ .Data.data.root_password }}
{{ end }}
{{ with secret (printf "secret/data/platform/%s/redis" (env "ENV")) }}
AP_REDIS_PASSWORD={{ .Data.data.password }}
{{ end }}
{{ with secret (printf "secret/data/platform/%s/activepieces" (env "ENV")) }}
AP_ENCRYPTION_KEY={{ .Data.data.encryption_key }}
AP_JWT_SECRET={{ .Data.data.jwt_secret }}
AP_FRONTEND_URL={{ .Data.data.frontend_url }}
{{ end }}
