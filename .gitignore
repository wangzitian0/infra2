# Environment files (keep only .env.example and .env.*.example)
.env
.env.*
!.env.example
!.env.*.example
!**/.env.example
!**/.env.*.example

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
.venv/
.venv-docs/
venv/
ENV/
env/
.virtualenv/

# UV (Python package manager)
# Note: uv.lock should be committed for reproducible builds

# Testing
.pytest_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.hypothesis/
playwright-report/
test-results/
# docs/ssot/coverage-baseline.json is intentionally tracked (coverage no-regression floor)
/coverage/

# Ad-hoc agent/debug run logs at the repo root
/run_log*.txt

# Environment variables
.env
.env.local
.env.*.local

# IDEs and editors
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Logs
*.log
logs/

# Temporary files
tmp/
temp/
*.tmp

# MkDocs build output
.site/
docs/.site/

# Cloudflare Wrangler local cache/state
.wrangler/
**/.wrangler/

# Codex local agent workspace state
.codex/

# `.claude/` is local agent runtime state and stays ignored -- except the
# skill discovery address. A host reads `<repo>/.claude/skills/`; it does
# not read `<repo>/skills/`, so the committed symlinks there are what makes
# the vendored skills reachable at all (dev_env#120).
# `.claude/*` rather than `.claude/`: git never descends into an ignored
# directory, so a bare `!.claude/skills` under it would match nothing.
.claude/*
!.claude/skills
.cache/
.envrc
.omo/
/handover*.md
