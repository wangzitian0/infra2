# Bootstrap service-level environment variables
# Copy to .env.production and fill in values

# 1Password Connect (get from 1Password)
OP_CONNECT_TOKEN=

# Vault unseal keys (stored in 1Password, referenced by item path)
OP_VAULT_ID=
OP_ITEM_ID=
